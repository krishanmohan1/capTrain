public class CheckPositiveOrNot {
    public static void main(String[] args) {
        int num = 0;
        String res = num > 0 ? "Positive No." : num == 0 ? "Neutral No" : " Negative";
        System.out.println(res);
    }
}
